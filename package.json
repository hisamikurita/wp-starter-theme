{
  "name": "wp-env",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "wp-env": "wp-env",
    "import": "gzip -d sql/wpenv.sql.gz | npm run wp-env run cli wp db import sql/wpenv.sql",
    "export": "npm run wp-env run cli wp db export sql/wpenv.sql; gzip -f sql/wpenv.sql",
    "wp-start": "npm run wp-env start",
    "wp-setup": "npm run wp-start && npm run import"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@wordpress/env": "^5.15.0"
  }
}